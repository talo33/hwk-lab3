import * as React from 'react';
import * as ReactDOM from 'react-dom';

export default function Element41() {
  const style = {
    borderStyle: "solid",
    borderColor: "green",
    borderWidth: "1px",
    backgroundColor: "#d0f0c0",
    color: "green",
    textAlign: "center",
    fontSize: "15px",
    backgroundColor: "#d0f0c0",
  }

  return (
    <div style={style}>
      Green is the prime color of the world
    </div>
  );
}
